if (TARGET wolfssl)
    log_target_found(wolfssl)
    return()
endif()

find_package(wolfssl QUIET)
if (wolfssl_FOUND)
    log_module_found(wolfssl)
    return()
endif()

find_package(PkgConfig)
pkg_check_modules(wolfssl REQUIRED wolfssl)
if (wolfssl_FOUND)
    log_module_found(wolfssl)
    target_include_directories(dottorrent PUBLIC ${wolfssl_INCLUDE_DIRS})
    target_link_libraries(dottorrent PUBLIC ${wolfssl_LINK_LIBRARIES})
    return()
endif()
